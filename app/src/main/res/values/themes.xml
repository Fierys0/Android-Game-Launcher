<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FumboGameLauncher" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/light_bg</item>
        <item name="colorSurface">@color/light_surface</item>
        <item name="colorOnSurface">@color/light_text_primary</item>

        <!-- Bottom Nav Custom Style -->
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>
    </style>

    <style name="Widget.App.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="itemActiveIndicatorStyle">@style/Widget.App.Indicator.Underline</item>
        <item name="labelVisibilityMode">labeled</item>
        <item name="itemIconTint">@color/nav_selector_color</item>
        <item name="itemTextColor">@color/nav_selector_color</item>
        <item name="itemRippleColor">@null</item>
        <item name="itemBackground">@drawable/bg_nav_item</item>
        <item name="android:background">@drawable/glass_nav_bg</item>
    </style>

    <!-- Custom underline indicator (Hidden indicator to use custom background instead) -->
    <style name="Widget.App.Indicator.Underline" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@android:color/transparent</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?android:attr/windowBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_games</item>
        <item name="postSplashScreenTheme">@style/Theme.FumboGameLauncher</item>
    </style>
</resources>
